Drop a team-recorded 15–25 second `.mp4` walkthrough here, optionally with a
`<clip>.mp4.meta.json` sidecar describing zone regions, for example:

```json
{"regions": [{"zoneId": "C", "box": [0.55, 0.4, 1.0, 1.0]}]}
```

Raw source video is never served by the dashboard. See `data/ATTRIBUTION.md`.
