Drop a team-recorded 8–15 second 16 kHz mono `.wav` radio clip here.

If no local ASR model is served, also drop an operator transcript sidecar named
`<clip>.wav.transcript.txt` (or `.transcript.json`). RiskTwin will never invent
a radio call. See `data/ATTRIBUTION.md`.
